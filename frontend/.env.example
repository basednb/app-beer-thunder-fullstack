# URL base da API (sem /api no final — o código já adiciona o prefixo /api)
# Local: http://localhost:3000
# Produção: https://app-beer-thunder-fullstack.onrender.com
VITE_API_URL=http://localhost:3000
